[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "midagri-comercio-exterior-pipeline"
version = "0.1.0"
description = "Pipeline liviano para cuadros de comercio exterior agrario de MIDAGRI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "polars==1.30.0",
  "pyarrow==20.0.0",
  "deltalake==0.18.2",
  "lxml==5.4.0",
  "openpyxl==3.1.5",
  "fastexcel==0.13.0",
  "pydantic==2.11.4",
  "pydantic-settings==2.9.1",
  "python-dotenv==1.1.0",
  "typer==0.15.4",
  "loguru==0.7.3",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
